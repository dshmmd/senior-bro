# 2026-07-09 — RF-1 done + RF-2 slice 1 (verify scripts gate CI)

Refactor-plan execution started (owner answered all §6 decision points — recorded
in REFACTOR.md: interview-bundle pricing, single-locale deploys EN *or* FA with a
later FA theme, adaptive-by-level celebration tone, Admin v2 moved early in P1).

## RF-1 ✅ — commit the in-flight work + branch hygiene

- The dirty tree (shipped-but-uncommitted R30 transcription, R37/R39 onboarding
  redesign, admin-entitlement fix) verified green (`make check` + `make e2e`) and
  committed as `7a30765`; plan docs as `9d1e04d`. Pushed.
- CLAUDE.md agent rule **3a** added: `main` never left dirty at a gate; refactor
  epics on short-lived branches merged when green.

## RF-2 slice 1 ✅ — integration suite in CI

- `server/test/integration/verify-scripts.test.mjs` runs all 12
  `scripts/verify-*.mjs` sequentially under `node --test` against an isolated
  `senior_bro_itest` DB, wiped between scripts (e2e/prepare.mjs trick: drop
  public+drizzle schemas → server re-migrates on boot). `verify-ph13` predates the
  self-boot pattern, so the runner boots its hosted server on :4791 itself.
- Wired: `npm run test:integration`, `make test-integration`, **`make check`**,
  CI step. Suite ≈ 24s. eslint `allowDefaultProject` extended for the new dir.
- **Sabotage-proven:** flipping `FREE_IMPRESSION_LIMIT` 3→4 failed 3 tests.
- Merged ff-only from `rf2-integration-suite` as `7aa8a7d`, pushed.

## Gotchas for the next agent

- Scripts share hardcoded ports (4795×3, 4796×2) — the runner must stay
  **sequential** (node --test default within one file).
- `gh` CLI is not installed on the owner's machine — CI status can't be checked
  from the shell; local `make check` mirrors CI steps.
- Docker was down at session start — `open -a Docker` + poll `docker info`.

## Next

**RF-2 slice 2** (port scripts into focused test files with a shared harness,
delete each script as its port is proven) — can interleave with **RF-3** (server
monolith split), which slice 1 now makes safe. Then RF-4/RF-5 per REFACTOR.md §5.
